# ROS2 技术周报 — 2026-08-31 至 2026-09-06

## 总判断

本周 ROS 2 的主旋律是**「ROSCon 前最后冲刺：Lyrical 冻结同步落地、治理层换届投票启动、以及『依赖版本钉死』这一老大难问题第一次被拿到 PMC 桌面上寻求体系化解法」**。最值得关注的三件事：其一，**Lyrical 本周 freeze + sync**（9/1 PMC 纪要明确），同时 Humble sync 排期下周、Jazzy 正在清理回归、Rolling 同步自动化 PR 进入评审——四条发行线同时在动，是 ROSCon 前典型的发布管理高峰；其二，**PMC 换届选举选票已发出**（skye.galaxy、jmachowinski、andrew_symington 拟任委员，nuclearsandwich、audrow 转 emeritus），且第二批 committer 将首次采用「scoped access」限定范围写权限——上周预告的治理变革本周进入执行阶段，Bazel 工作组则明确推迟到 andrew_symington 正式入委之后；其三，**rmw_fastrtps#906 的依赖钉版本讨论**触发了 cottsay 提出的更重要方案：启用 package format 3 中早已存在但基本无人使用的**版本兼容标志**，把「构建时依赖版本」传导进下游 Debian/RPM 包约束，从体系上杜绝「只更新一个包、整个栈 ABI 崩掉」的事故。工程侧，SIG Physical AI 开始定期向 PMC 汇报，其 WG1 的零拷贝 rosidl 概念已跑出 proof of life（4MB 图像、Fast DDS 共享内存、约 1 kHz / 1 ms）。

---

## 本周关键信号

### 1. 四条发行线同时进入发布管理高峰：Lyrical 本周 freeze+sync，Rolling 同步自动化进入评审

**发生了什么：** 9/1 PMC 纪要的 Release Management 环节给出了罕见的「全线状态快照」：

- **Lyrical（sloretz）：本周 freeze + sync**——Lyrical GA 后第三次 sync 级别的动作；
- **Humble（christophebedard）**：上次 sync 8/7，已到期，**计划下周 sync**，待当前 rebuild 稳定；
- **Jazzy（marcogg）**：上次 sync 还是 6/18，正在逐一清理回归以准备 sync；
- **Kilted（cottsay）**：上次 sync 8/21，471 个包、**0 回归**；
- **Rolling（yadunund）**：同步自动化的两个 PR（ros_buildfarm#1151、ros_buildfarm_config#396）在飞，claraberendsen 评审后将部署到测试场；**包通过率阈值定为 80%**，即允许最多约 200 个包被移出——与 yadunund 手动 sync 时移除过的最大数量相当。

**为什么值得关注：** Lyrical freeze+sync 意味着上周 Nav2 Lyrical 版（1.5.x）等生态成果将随本次 sync 进入官方软件源，Lyrical 生态「可装可用」的完整度再上一台阶。更结构性的是 Rolling 同步自动化：阈值 80% 的设定是把「人肉判断」第一次量化成「机器可执行的策略」，直接决定 Rolling 仓的长期健康度与维护者负担。Jazzy 两个半月未 sync 正在清回归，说明 LTS 的发布质量门正在收紧——这对把 Jazzy 用于生产的团队是好消息，但也意味着近期别指望 Jazzy 源快速拿到新修复。

**影响层面：** 发行版节奏 / 发布管理 / CI 基础设施

**下一步盯什么：** Lyrical sync 公告与回归清单；Humble 下周 sync 是否如期；Rolling 自动化 PR 部署到测试场后的首次自动 sync 结果；80% 阈值是否会随运行数据调整。

---

### 2. rmw_fastrtps#906 引出体系性方案：用 package format 3 的版本兼容标志杜绝「局部升级炸全栈」

**发生了什么：** MiguelCompany 在 PMC 提出 rmw_fastrtps#906——**钉死依赖版本**。动机很具体：某个共享包发生 ABI break 后，只更新了 `rmw_fastrtps` 的用户会发现整个栈在脚下崩掉。讨论迅速升级：

- cottsay 指出钉版本造成「两处需同步维护」，而现有发布自动化不会更新这些 pin，catkin_pkg prepare-release 也不会，「在几百个包的发布季会非常痛苦」；
- clalancette 建议改用「大于等于」约束而非等号，并指出这是**全栈问题**，不是 Fast DDS 特有；
- **cottsay 提出更优的通用路径：package format 3 已有版本兼容标志（version compatibility flag）但基本没被使用**。若正确打通，包的依赖可以被锁定到「构建时的版本」，并强制传导到下游 Debian/RPM 包约束中——「局部更新搞坏系统」将变得不可能。cottsay 主动提出与 MiguelCompany 合作把这件事做成体系方案。

**为什么值得关注：** 这是 ROS 2 二进制分发模型的深层缺陷第一次被 PMC 级人物认领：apt 世界里 ROS 包之间只有「依赖名」没有「ABI 代际」约束，任何一次 sync 的不完整更新都可能产生运行时崩溃。若 version compatibility flag 方案落地，影响面是整个 buildfarm 的打包逻辑与所有发行版的升级安全性——比上周的 ABI 自动检查原型（tomoyafujita）更进一步：一个管「防出错」，一个管「错了也炸不了」。

**影响层面：** 工程化 / 分发与打包体系 / ABI 稳定性

**下一步盯什么：** cottsay 与 MiguelCompany 的协作产出形态（REP？buildfarm 改动？）；version compatibility flag 在 Debian 打包链路的实际传导验证；与 ABI 检查原型（帖子 57477）是否合流为「ROS 2 ABI 治理」组合拳。

---

### 3. PMC 换届选票发出 + 第二批 committer 将首次采用 scoped access，Bazel WG 正式推迟

**发生了什么：**

- **选举选票已发出**（9/1 时已有 7 票，需全体 PMC Constituents 三分之二多数）：skye.galaxy、jmachowinski、andrew_symington 拟任 PMC 委员；nuclearsandwich、audrow 转 emeritus。第二批拟任 committer 的选票将**在各自仓库范围（scope）确认后**单独发出，mjcarroll 本周内与候选人确认意向仓库，目标 ROSCon 前完成；
- **Bazel 工作组正式 deferred**：等 andrew_symington 成为正式 PMC 委员后再启动——上周预告的组织化进程节奏明确；
- **REP 158（OpenUSD 仿真资产互操作规范）进入批准流程**：mjcarroll 正在组装批准材料包（REP 文本 + 参考实现 + Discourse 讨论链接），Physical AI SIG 与 Gazebo PMC 均已评审、Gazebo 无异议。明确放在 ROS 而非 Gazebo 之下，因为它是比单一仿真器更宽的仿真标准（O3DE、Isaac 都重度使用 USD）。目标 ROSCon 前过线，会上讨论；
- TGC 非正式会议讨论 **AI 抓取与 AI 贡献政策**：参考了 Debian 刚结束的同类投票（结论与 OSRA 现有政策接近）；clalancette 认为「哪些部分由 AI 生成、用的什么模型」是值得保留的信息，归属标注或放 PR 描述而非 commit sign-off。

**为什么值得关注：** 「scoped access」落地执行改变了 ROS 多年来的权限惯例，配合换届，ROSCon 前 PMC 将完成近年来最大一次治理结构更新。REP 158 若获批，是 ROS 历史上第一个「仿真资产格式」级别的 REP，其意义不在 Gazebo 是否立即采用（明确不会），而在为 USD 生态（Isaac/O3DE）与 ROS 工具链之间建立官方互操作约定。AI 政策讨论则关系到每个贡献者未来的 PR 填写义务。

**影响层面：** 治理 / 标准（REP）/ 社区政策

**下一步盯什么：** 选举结果（ROSCon 前）；scoped access 首批执行案例；REP 158 是否在 ROSCon 前获批及会上反馈；AI 归属标注政策是否形成文字。

---

### 4. SIG Physical AI 开始定期向 PMC 汇报：零拷贝 rosidl 跑出 proof of life，旗舰参考应用路线浮现

**发生了什么：** SIG Physical AI 首次以固定议程向 PMC 汇报（yadunund 报告），各工作组状态：

- **WG1（接口/互操作/传输）**：**零拷贝 rosidl 概念达到 proof of life——4 MB 图像经 Fast DDS 共享内存跑到约 1 kHz、1 ms**，仍在排查额外延迟来源；正在评审 REP 158；**Hardware Configuration Description Format 提案**（融合 URDF、SDF 与硬件特定参数）的 Discourse 帖子待发；3D LiDAR 消息提案停滞，触觉提案在收集反馈；
- **WG2（数据记录与回放）**：基于 Rosetta（rosbag2 + ROS 话题 + LeRobot 生态的社区集成，使用 Parquet）构建；
- **WG4（应用）**：demo 从 Kilted 迁移到 Lyrical；AIC 插线缆 demo 在 draft PR；下一步接 WG3 推理管线、采用 CUDA buffer + ONNX 转换；
- **路线图**：正在筹划一个**更复杂形态的参考应用**（双臂移动底座或人形），作为与下一发行版对齐的旗舰演示平台；
- 同控制器驱动 LeRobot SO-101 与 UR 机械臂的 demo 展示了「从爱好者硬件到工业硬件只差配置与 launch 参数」的路径。

**为什么值得关注：** 上周 LeRobot↔ROS 桥接「厂商级 benchmark」之后，本周是组织侧的对应动作：SIG 从「兴趣小组」升级为「定期向 PMC 汇报、产出标准与参考实现」的正式结构。WG1 的 1 kHz/1 ms 零拷贝数据点尤其重要——它把「rosidl 层零拷贝」从路线图变成了可测量的事实，后续大图像/点云管线（感知→策略推理）的性能预期可以基于此校准。「旗舰人形参考应用」若兑现，将直接定义下一个 ROS 发行版的 Physical AI 叙事。

**影响层面：** 战略层 / 具身智能基础设施 / 中间件性能

**下一步盯什么：** 零拷贝 rosidl 额外延迟来源的排查结论与是否进入主线 rosidl；Hardware Configuration Description Format 提案帖发布；WG2 Rosetta/Parquet 管线的可用性；旗舰参考应用的形态选择与时间表。

---

### 5. Buildfarm 双信号：Zenoh 全线转绿 vs Windows 多播测试再现回归

**发生了什么：** 9/1 PMC 纪要的 Buildfarm 环节：

- **Zenoh 恢复全绿**——yadunund 与 ahcorde 持续投入的结果。Rolling 的绿色数量「低两位数」，绝对值不好但趋势向上；本月整体绿度除 Lyrical 外普遍下降；
- **Windows 11 上 `ros2_multicast` 的 `test_api` 回归重现**（2025 年出现过）：表现为 buffer space 故障、波及大量多播测试。工作假设：Windows 上基于 SIGTERM 的 teardown 清理不干净，**多播 socket 在宿主机上逐渐耗尽**——claraberendsen 在 Lyrical 工作中撞过同一堵墙；Windows 用进程隔离而非容器，顺序任务共享宿主机网络，清理不彻底会累积。出问题的 agent 日志显示连续至少五天跑重复 nightly。mjcarroll 将扩展 CI 清理流程并寻找内部修复；
- 另有一个全 Linux 平台（含 RHEL/aarch64）的 `rmw_ret_t` 返回值未使用警告，源头是两天前合入某 RMW 实现的测试，clang 更严格，修复中。

**为什么值得关注：** 一正一反都很说明问题。Zenoh 转绿意味着 rmw_zenoh 距离「可推荐的 Tier 1 候选」又近一步，与本周 fastdds_transport_viz 这类 Fast DDS 专用诊断工具的出现形成有趣对照——两条 RMW 路线都在补「可观测性」课。Windows 多播回归则再次印证上周 Windows CI「假绿」讨论的核心结论：Windows 是 ROS 2 CI 体系里结构性最薄弱的一环，且问题已从「测试没跑」升级到「测试把宿主机跑坏」。

**影响层面：** 工程化 / CI 与平台基线 / RMW 中间件

**下一步盯什么：** mjcarroll 的 CI 清理扩展方案是否消除多播回归；Zenoh 绿度的可持续性；Rolling 绿度回升速度。

---

### 6. 诊断体系大讨论：ros2_medkit 解释「为什么不扩展 /diagnostics」——状态流 ≠ 故障模型

**发生了什么：** ros2_medkit 团队（9/4）发布长文，正面回答「为什么不扩展 diagnostic_msgs / diagnostic_updater / aggregator」这个被问了一年（包括 ROSCon 审稿人）的问题。核心论点：**status 是信息，fault 是可查询、可操作的模型，后者无法通过给前者加字段得到**。具体论证：

- REP 107 体系是「实时状态流」：aggregator 按 name 只保留最新一条、覆盖式更新，rqt 只缓存 30 帧——**没有「故障」这个东西的身份**：不知何时开始、复发几次、是否仍活跃、谁确认过；
- 很多故障源根本不是 ROS 节点（电机驱动里的 MCU、独立固件的传感器），driver 静默时 aggregator 只能说 STALE；
- 「它还活跃吗？确认它、改它背后的配置、把当时的录包给我」——这些操作需要一个**独立于 ROS graph、持有带地址与动词的故障记录的服务**。ros2_medkit 的实现：`Fault` 消息（fault_code、AUTOSAR DEM 式防抖生命周期 PREFAILED→CONFIRMED→HEALED→CLEARED、occurrence_count）、确认时自动抓 freeze frame（16 个话题）+ 写 MCAP ring buffer（故障前 5s 后 1s）、SQLite 持久化、挂到 SOVD（ISO 17978）实体树、全套 REST/SSE API 不需要加入 DDS graph；
- `/diagnostics` 仍是输入：bridge 把 OK/WARN/ERROR/STALE 映射为 PASSED/故障事件，现有 diagnostic_updater 节点零改动即可获得故障码、生命周期与历史。

**为什么值得关注：** 这是连续第四周「可观测性/可运维」主线的延续（ros2probe→ros2_pulse→官方拟删旧 topic statistics→本周故障管理），但层次不同：前几周解决「看得见」，本周解决「看得见之后如何形成可操作的运维闭环」。AUTOSAR DEM 防抖 + SOVD 实体树 + REST 外暴露的组合，明显是汽车行业诊断方法论向 ROS 的移植——与 ROS-Industrial 渗透率提升的大背景同频。作者抛给社区的问题（「fault record 是否应成为共享 ROS 消息」）可能演化为新的 REP 级讨论。

**影响层面：** 工程化 / 诊断与运维体系 / 生产部署

**下一步盯什么：** ROSCon 上该议题的 talk 与社区反馈；fault 记录标准化的呼声是否成形；ros2_medkit 与官方诊断栈的边界讨论结果。

---

### 7. ROS 2 安装入口之争：一条命令安装 apt 路径的提案引发信任边界讨论

**发生了什么：** MrBearing 提案为 apt 安装路径提供「一条命令安装器」（9/3 帖为回应质疑的第二轮论述）。质疑点是 `curl | sh` 反模式；作者的反驳相当扎实：

- 现状的信任边界本来就是「从项目控制的 URL 经 HTTPS 下载 .deb 并 sudo dpkg -i」——脚本并不扩大它，且脚本可读；
- 历史事故清单：2019 安全事件轮换 key、2021 apt-key→signed-by 迁移、**2021-05 key 过期（官方 Docker 镜像与 setup-ros 在假期周末全红）**、**2025-06-01 key 再次过期引入 ros-apt-source**——每次事故都是全社区的 Dockerfile/CI/Ansible 副本同时坏，然后各自修；
- 真正诉求是「**一个有人维护的单一入口**，让文档、Dockerfile、CI 都指向它」：可选形态包括 ros2-apt-source .deb 的无版本稳定 URL（peci1 此前已要求）、把 ros2-apt-source 推进 Ubuntu 官方源（`sudo apt install ros2-apt-source`，最干净但工作量最大）、或 ros2/ros-infrastructure org 下托管的「下载-阅读-运行」脚本。

**为什么值得关注：** 这接上了上周「ROS 融入主流软件工程工具链」（ros-conan、Bazel WG、Windows CI）的主线——安装入口是新人体验与 CI 稳定性的第一道关。2021/2025 两次 key 事故的「全社区同时坏、各自修」模式，本质是缺少单一维护入口的结构性后果。作者明确表示希望 PMC 认领所有权——这与本周 rosdistro 内容准则讨论（emersonknapp：「我们要么立政策，要么承认没有」）同属「社区基础设施的权责清晰化」趋势。

**影响层面：** 工程化 / 安装与分发入口 / 新人体验

**下一步盯什么：** PMC 是否认领该入口；无版本稳定 URL 与「进 Ubuntu 源」两条路径的可行性结论；ROSCon 基础设施话题中的相关讨论。

---

## 核心框架更新

### 发行版与发布管理
- **Lyrical**：freeze + sync 本周执行（详见信号 1）。
- **rclcpp 33.1.0（8/31）**：内含 **#3243「拒绝不兼容的参数范围约束」**——参数声明时若默认值落在 range 约束之外将被直接拒绝，属于行为收紧类变更，依赖「宽松声明」的代码升级后可能暴露历史配置错误。
- **rcl 10.5.2（8/31）**、**rclpy 11.0.3（9/1）**、**ros2cli 0.42.0（9/1）**、**launch 3.10.1（9/2）**：常规滚动发布。launch #976 修复「launch prefix 不再覆盖 logger 名」；ros2cli #1283 回移了非标准 interface 位置的修复。

### rclcpp / rclpy / rosbag2
- **rclcpp 本周 8 commits**：除上述版本节点外，主线是「include 卫生」——三个测试目录的 Granular includes / IWYU 清理（#3249/#3250/#3252），延续近几周的头文件减负方向，直接收益是编译时间；#3248 消除 ClocksState 中不必要的 shared_ptr 分配（微优化）；#3253 补 const 正确性。
- **rclpy**：#1680 引入 ament_mypy——**rclpy 开始系统性类型检查**，与 Nav2 上周「全局移除 mypy」形成方向相反的对照，说明「Python 侧类型标注」在核心库与生态项目间的成本收益判断并不一致。
- **rosbag2 4 commits**：#2470 增强 action 状态话题的处理与过滤；#2482 修复回放结束后固定频率 /clock 仍在发布的问题（回放污染实测环境的经典坑）；另注意 PMC 讨论的 **#2476（混合序列化格式回放）**：将从 `SequentialReader` 删除两个虚函数——**对子类构成 API break，仅限 Rolling 不回移**，并为 `SerializedBagMessage` 增加 `serialization_format` 字段（共识：暂不动 `rclcpp::SerializedMessage`，为未来 protobuf 等序列化进核心留了一个明确待办）。

### launch / rosidl / rmw
- launch 3.10.1 见上；rosidl、rmw、rcl_interfaces、demos 本周无新提交。
- **Fast-CDR v2.2.8（9/1）**发布；Fast-DDS 本身仅一个文档构建修复（#6521）——中间件层本周平静，注意力在 RMW 打包约束（信号 2）与 Zenoh 绿度（信号 5）。

**工程意义：** 核心库延续「GA 后消化期」的低功能密度、高卫生密度特征；真正会改变行为的只有 rclcpp #3243（参数范围收紧）与 rosbag2 #2476（Rolling 限定 API break）。升级 Rolling 的团队两条都需要过一遍。

---

## 生态项目更新

| 项目 | 本周动态 | 工程意义 |
|------|---------|---------|
| **Nav2** | 7 commits：**#6435 修复 Costmap2D::costmap_ 并发访问**（线程安全）；#6329 KeepoutFilter 抹除未知单元格修复；#6420 axis_goal_checker 路径截断过短修复；theta_star 两个修复（#6433 未知单元格计费一致化、#6425 启发权重同步）；#6442 smac 2D 警告；#6443 rotation shim controller 文档 | #6435 是本周生态侧最重要的正确性修复——costmap 并发访问是生产线上的隐患型 bug；整体延续「加固正确性」主线，随 Lyrical sync 落地 |
| **ros2_control** | 6.10.0（9/2）：修复 view_controller_chains（#2026）、改用 libboost-devel（#3583）、pre-commit 升级 | 常规迭代，无高风险变更 |
| **MoveIt2** | 10 commits：**修复 Ubuntu 26.04 上 clang 构建**、清理 boost 依赖、Docker 构建工作流重构、moveit_setup_assistant 头文件导出修复（#3844）、clang-tidy 修复 | 主线是「为 Ubuntu 26.04（Resolute）平台基线迁移扫清构建障碍」，与 Gazebo 弃 FreeImage、macOS Intel 退场同属下一轮平台迁移的前置工程 |
| **Gazebo / gz-sim** | 5 commits：**#3936 引入 EcmGuard——EntityComponentManager 的线程安全 RAII 访问**；#3850 避免拷贝缓存的物理步输出（性能）；#3951 修复模板化相等运算符的 trait | EcmGuard 说明 ECM 线程安全在被系统性收口，对写自定义系统/插件的开发者是正面信号；GSoC 学生完成 Gazebo reset 核心系统 QA（RL 训练场景受益） |
| **micro-ROS** | 无新提交 | 平稳期 |
| **诊断/观测工具** | **fastdds_transport_viz 发布**（9/6，atinfinity）：从 Fast DDS discovery 数据预测每个 writer→reader 对实际使用的传输方式（UDPv4/v6、TCP、SHM、零拷贝 data-sharing）并解释原因；--stats 实测速率/延迟；Jazzy/Kilted/Rolling 验证 | 补齐 Fast DDS「传输层黑盒」的可观测性——SHM/zero-copy 是否生效从此可查证，不再靠猜；与 ros2_medkit 同属本周「可运维性」双响 |
| **新硬件/客户端** | Bimo 开源双足套件（sim-to-real）；SnakeROS——MicroPython 纯 Python ROS 2 客户端 | 生态丰富度信号，暂不改工程判断 |

---

## 工程化观察

- **回归风险**：本周最大单点是 Nav2 #6435（costmap 并发）——已修复，随下次 sync 落地前，使用 Rolling/Lyrical 源码构建的团队应确认是否包含。rclcpp #3243 的参数范围收紧可能在升级后暴露「一直就错但没人管」的参数声明。rosbag2 #2476 的 SequentialReader API break 仅影响 Rolling 上的子类实现者。
- **平台迁移前奏密集**：MoveIt2 修 Ubuntu 26.04 clang 构建、Gazebo 弃 FreeImage 并对齐 Resolute、Homebrew/Gazebo 弃 macOS Intel（ROS 2 侧尚无对应公告，需继续盯）、Windows 多播 socket 耗尽回归——四条线共同说明 **2027 周期的平台基线迁移已实质启动**，维护多平台 CI 的团队现在就该盘点对 Ubuntu 26.04 与 macOS 架构的依赖。
- **「权责清晰化」成为横向主题**：rosdistro 内容准则（mjcarroll 起草中）、ROS 2 安装单一入口提案、scoped access committer 权限、version compatibility flag——四件看似无关的事共享同一个方向：把过去靠惯例运转的环节变成有人认领、有文字可依的制度。对下游用户的直接收益是可预期的稳定性。
- **可观测性连续第四周有新货**：ros2_medkit（故障管理闭环）+ fastdds_transport_viz（传输层可见性）。判断不变：社区主线已从「功能可用」转到「可观测、可运维」，且开始触及「标准化」（fault record 是否该成共享消息）。

---

## 前沿议题

1. **Physical AI 的「ROS 官方叙事」正在成形**
   SIG PAI 定期汇报 + 零拷贝 rosidl proof of life（1 kHz/1 ms/4MB）+ 同控制器跨 SO-101/UR + 旗舰人形参考应用规划——组织、性能数据、演示、路线图四要素齐备。叠加本周新闻「NVIDIA 计划收购 Hugging Face 并保持平台开放」，LeRobot 生态的底座可能易主，SIG PAI 作为 ROS 侧对接口的重要性反而上升。下周 ROSCon（9/22–24 Toronto）是该叙事的第一波集中释放点。

2. **USD 正从「仿真器内部格式」变为「ROS 级标准」**
   REP 158 放在 ROS 而非 Gazebo 之下获批在即，官方理由是 O3DE/Isaac 都重度使用 USD。这是 ROS 官方第一次为「非自家格式」立 REP——信号意义大于技术意义：ROS 在仿真资产层选择「对接主流」而非「另起炉灶」。做数字孪生/仿真资产管线的团队应评估 REP 158 约定与自有资产格式的对齐成本。

3. **诊断方法论的汽车行业移植**
   ros2_medkit 把 AUTOSAR DEM 防抖 + SOVD（ISO 17978）实体树整套搬进 ROS 2，并提供免 DDS 的 REST/SSE 外暴露。若 ROSCon 反馈积极，「fault record 标准化」可能成为下一个 REP 级议题，对车队运维与功能安全相关场景（ISO 26262 相邻领域）影响直接。

---

## 可继续深挖的问题

1. Lyrical 本周 sync 的实际回归清单与 Nav2 1.5.x 进入官方源后的首批部署反馈如何？
2. cottsay 的 package format 3 version compatibility flag 方案会以什么形态落地（REP/buildfarm 改动/打包脚本），与 ABI 检查原型是否合流？
3. Rolling 同步自动化（80% 阈值）首次实际运行的移除名单与误伤情况？
4. PMC 选举与 scoped access committer 名单是否在 ROSCon 前全部落定？Bazel WG 何时正式启动？
5. 零拷贝 rosidl 的额外延迟来源查明了吗？该能力进入主线 rosidl 的路径与时间表？
6. Windows 多播 socket 耗尽的 CI 清理修复是否有效？ROS 2 何时跟进 macOS Intel 退场声明？

---

## 下周优先跟踪

- **ROSCon Global 2026（9/22–24 Toronto）+ 9/25 Google Toronto hackathon** 的会前信号：演讲材料、REP 158 批准、PMC 选举结果；
- Lyrical sync 公告与 Humble sync 是否如期；
- Gazebo M 命名投票 9/17 截止结果；
- 上周遗留：`ros2 topic hz` C++ 化与旧 topic statistics 删除 PR 的合入状态（本周 PMC 纪要未提及，仍待验证）。

---

*数据来源：ROS PMC 纪要（2026-09-01）、Discourse 帖子（57843/57859/57868/57900 等）、GitHub commits/releases API（窗口 2026-08-31 ~ 2026-09-06）、ROS News of the Week（8/31）。原始数据存档于 `data/2026-09-06/`。*
