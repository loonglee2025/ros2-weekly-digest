# ROS2 技术周报 — 2026-08-17 至 2026-08-23

## 总判断

本周 ROS 2 的主旋律是**「两个发行版同时进入大规模同步交付、具身智能（Physical AI）正式被 OSRA 组织化、以及数据/序列化/观测性议题集中升温」**。最值得关注的是三件大事：其一，**OSRA 正式成立 Physical AI 特别兴趣小组（SIG PAI）**，本周放出一个集成 LeRobot + ROS + 多个机器人仿真器的 demo 仓库并公开数年路线图——这是具身智能从「生态散点」走向「官方组织化」的标志性一步；其二，**Kilted Kaiju 迎来发布后首次大规模同步（8/21）**，471 个包（39 新增 / 451 更新）进入主 apt/dnf，easynav 导航栈、MOLA SLAM、roboplan 运动规划等一批重量级新包落地；其三，**Lyrical 同步（8/19）按上周预告完成**，104 个新包 + 150 个更新入库，MoveIt 2.15、Nav2 dwb 系列等直接受益。与此同时，「零拷贝/共享内存」这条主线再进一层（rosidl::Buffer CPU 共享内存后端讨论 + Fast-DDS buffer 复用补丁），且数据/观测性工具出现一轮井喷（ros2probe、ROS 2 Inspector、Foxglove Agents、ros-opentelemetry 回暖、YERP+rosbag2）。核心框架侧，rclcpp 新增依赖 node 接口的 `wait_for_message` API、rosbag2 录制端切换到 EventsCBGExecutor、Nav2 发布 Humble 补丁版 1.3.13 并伴随 MPPI 改动反复。整体看，本周是「发行版交付 + 战略组织化 + 数据基础设施」三线并进的密集一周。

---

## 本周关键信号

### 1. OSRA 成立 Physical AI SIG（SIG PAI）：LeRobot + ROS + 多仿真器 demo 仓库 + 数年路线图

**发生了什么：** OSRA（Open Source Robotics Alliance）新的 Special Interest Group on Physical AI（SIG PAI）本周正式对外发声，并放出两个实质性产物：
- 一个 **GitHub demo 仓库**，示范如何把 **LeRobot + ROS + 多个机器人仿真器**整合进一条数据/学习管线
- 一份 **未来数年的路线图** 及「如何参与」的说明

同时，ROS 官方周报确认了 SIG PAI 的定位：让 ROS 继续「随着 Physical AI 演进」——把 ROS 作为具身智能数据采集、策略训练、部署回环的公共底座。

**为什么值得关注：** 这是 Physical AI 议题第一次被 OSRA 以正式组织（SIG）而非个人博客/论文的形式承接。它直接回应了过去几周周报反复出现的「LeRobot 数据管线、rosbag/MCAP 数据资产化、具身数据导出」等散点信号——现在这些散点有了一个官方的组织入口和路线图锚点。对做具身/机器人学习的团队，这意味着「ROS 官方将基础设施资源投向量产具身数据闭环」，值得长期跟踪其 roadmap 落地。

**影响层面：** 战略层 / 组织治理 / 具身智能基础设施

**下一步盯什么：** SIG PAI demo 仓库是否沉淀为官方文档/教程；路线图各阶段里程碑时间点；与 LeRobot、rosbag2/MCAP、仿真器（Gazebo/MuJoCo）的整合深度。

---

### 2. Kilted Kaiju 首次大规模同步（8/21）：471 包入库，easynav / MOLA / roboplan 集中落地

**发生了什么：** cottsay 主持的 **Kilted sync 2026-08-21** 完成，**471 个包**进入主 apt/dnf（**39 新增 / 451 更新**，0 回归）。亮点新包：
- **easynav**（costmap-localizer、regulated-pp-controller、simple-localizer 等一整套）——新代导航栈正式进入 Kilted
- **MOLA SLAM 全家桶**：mola-lidar-odometry、mp2p-icp-core/viz、各学术数据集输入（Euroc/KITTI/KITTI360/Mulran/Paris-Luco）
- **roboplan**（Trossen Robotics 系）运动规划库：roboplan-ros-cpp/py、toppra、visualization、example-models
- 其余：mujoco-3d-lidar、onnxruntime-vendor、rtabmap-costmap-plugins、rosbag-timing-inspector、unbag、magnetometer-broadcaster 等
- 大量更新：ros2_control 控制器系升到 **5.17.0**、control-msgs 6.10.0、depthai 3.9.0、easynav 0.4.1

**为什么值得关注：** 这是 Kilted 发布后的首次大规模同步，标志着 Kilted 生命周期进入「持续交付」常态（与 Lyrical 的节奏对齐）。尤其值得注意的是 **easynav（新一代导航栈）、MOLA（SLAM）、roboplan（运动规划）** 三大类同时进主仓库，说明「导航-建图-规划」在 Kilted 上的可选生态显著扩容，对评估 Kilted 的团队是明确的选型窗口。

**影响层面：** 发行版层 / 生态交付节奏

**下一步盯什么：** 这些新包在 Kilted 上的 CI/运行稳定性；easynav 与 Nav2 的关系如何定位；roboplan 是否带动 MoveIt 生态在 Kilted 的交叉采用。

---

### 3. Lyrical 同步（8/19）完成：104 新包 + 150 更新，MoveIt 2.15 与 Nav2 dwb 系落地

**发生了什么：** 上周预告的 **Lyrical sync 2026-08-19** 按计划完成并打 tag `lyrical/2026-08-19`：**104 个新包 + 150 个更新**。亮点：
- **MoveIt 2.15.0** 全家桶（moveit-core/kinematics/planners/chomp/hybrid-planning 等）入库
- **Nav2 dwb 系**（dwb-core/critics/plugins）作为独立包进入 Lyrical，另有 costmap-queue 1.5.1
- 其他新包：crx-kinematics、fields2cover 2.0.0、mapviz-plugins、jsk-* 系列、ffw-moveit-config 等

**为什么值得关注：** 这验证了上周「247 待同步/48 回归 → 走向持续同步」的判断，且实际交付规模（104 新 + 150 更新）相当可观。MoveIt 2.15 与 Nav2 dwb 系同批落地，说明 Lyrical 上「规划+导航」生态在同步补强；对依赖 Lyrical 的团队，这批新包是评估迁移/升级价值的关键输入。

**影响层面：** 发行版层 / 发布与同步节奏

**下一步盯什么：** MoveIt 2.15 在 Lyrical 的稳定性反馈；dwb 系与 MPPI 在 Lyrical 的采用情况；后续同步（下一次 Lyrical/Kilted sync）的时间与规模。

---

### 4. rosidl::Buffer 共享内存后端讨论 + Fast-DDS buffer 复用补丁：零拷贝再进一层

**发生了什么：** 延续上上周的 rosidl::Buffer 讨论，本周出现两条具体进展：
- **Daisuke_Kato 提出「CPU 共享内存后端」**：针对大变长载荷（图像/点云），探讨在 rosidl::Buffer 层让「载荷存储本身共享、传输描述符」，而非只在传输层做 SHM；已有人在做（Qualcomm 的 buffer 实现、NVIDIA 之外的第一个）。讨论中 tfoote 确认这是合理扩展，与中间件内嵌 SHM 部分重叠但提供更细粒度控制
- **Fast-DDS buffer 复用补丁（#904）**：发现 `std::vector<uint8_t>(buffer_size)` 会零初始化整块内存（memset），导致发布耗时随 buffer 大小线性增长；补丁改为复用 FastBuffer 托管缓冲，显著减少额外分配与零初始化——直接改善 rosidl::Buffer 优化路径的性能

**为什么值得关注：** 这是「零拷贝/共享内存从传输层上移到类型系统层」这一长期主线（rosidl::Buffer 是核心载体）的持续推进。缓冲复用补丁解决的是真实且可测量的性能问题（发布耗时随 buffer 线性增长）；而 CPU SHM 后端若落地，将让大载荷在进程间零拷贝共享成为标准能力，与 GPU（CUDA IPC）侧呼应，对感知/点云密集的机器人系统有直接价值。

**影响层面：** 核心框架层 / 类型系统（rosidl） / 中间件（Fast-DDS） / 零拷贝

**下一步盯什么：** Fast-DDS #904 是否合并及性能数据；CPU SHM 后端是否有原型落地；Qualcomm buffer 实现进展；rosidl::Buffer 在 Lyrical/Jazzy 的 backport 需求。

---

### 5. protoros2：以 protobuf 为中心的「三态编排」中间件封装

**发生了什么：** ZhenshengLee 发布 **protoros2**——一个非侵入的中间件封装/编排引擎，让 ROS 2 无缝使用 Protobuf 序列化：
- **Proto Channel（零侵入快路径）**：运行时探测 RMW 序列化格式，原生支持 Protobuf 则直走 `rclcpp::SerializedMessage`，否则优雅回退 CDR（TypeAdapter）
- **Flat Channel（性能旁路）**：基于 **Iceoryx 共享内存** 的超低延迟 IPC，适配 ROS 2 executor 生态
- 两种 SSOT：`.msg` 为主或 `.proto` 为主（用 proto2ros AST 解析自动生成镜像 IDL）
- 内置企业级安全：编译期禁用参数服务/QoS override 等动态配置
- 建立在 rosidl_typesupport_protobuf、proto2ros、ros-central-registry（Bazel）之上

**为什么值得关注：** protobuf 在 ROS 2 的需求由来已久，protoros2 的价值在于「不重造轮子、做编排」，且主打 off-robot 线效率（车辆边/云端的载荷削减与数据转换成本）与 MLOps 生态（直接消费 protobuf bag）。这与本周「数据资产化、wire 效率、云边协同」的整体氛围高度同频，也再次印证 protobuf/替代序列化在 ROS 2 的实用化加速。

**影响层面：** 工程化 / 序列化 / 中间件 / MLOps

**下一步盯什么：** protoros2 在真实车队的采用；与原生 Protobuf RMW（rmw_ecal_proto_cpp）的成熟度；是否被上游或社区标准化吸收。

---

### 6. 观测性工具井喷：ros2probe（eBPF 无探针效应）、ROS 2 Inspector、Foxglove Agents、ros-opentelemetry 回暖

**发生了什么：** 本周数据/观测性方向出现一轮集中发布与回暖：
- **ros2probe**：drop-in 替换 rosbag2 / `ros2 topic`，用 **eBPF 被动抓包**读取内核侧 RTPS 副本，**不进 DDS 域**、不加 DataReader。实测零探针效应（记录近 GbE 满载对订阅者 0% 丢包 vs rosbag2 最高 75.5%）、观测 CPU 低至 1/7、内存 1.7MB vs 47MB。局限：SHM 传输是结构性盲区
- **ROS 2 Inspector**：**静态架构分析**工具，`ros2inspector scan/viz/nodes/graph/audit/validate`，扫描源码+launch 生成包/节点/话题关系图（可导出 mermaid），不替代运行时工具、与其互补
- **Foxglove Agents + 语义检索**：查询 `visual("stairs") AND (/anymal/imu/...>4)` 这类跨语义+数值检索；A/B 对比多次运行；Agent 侧栏 + 桌面 MCP；BYOS 自带存储
- **ros-opentelemetry 回暖**：2025 年发布的 OTel 分布式追踪库本周收到活跃开发询问，补齐 ros2_tracing（LTTng 底层）之上的厂商中立端到端可观测层

**为什么值得关注：** 这几条合起来指向「把 ROS 2 的可观测性从『自己加订阅者凑合看』升级为『被动、无干扰、可检索、可审计』」：ros2probe 解决探针效应这一结构性痛点，Inspector 解决未运行时的架构理解，Foxglove 解决数据语义检索，OTel 解决跨节点追踪。它们与上周的 Bagel 2.0、lint_launch 同属「数据资产化 + 可观测」浪潮，且相互可组合（ros2probe 出 MCAP、Foxglove 检索、OTel 追踪）。

**影响层面：** 工程化 / 可观测性 / 数据工具链

**下一步盯什么：** ros2probe 是否覆盖 SHM 传输与多 RMW；Inspector 对动态拓扑/插件的建模；Foxglove 语义检索的数据隐私与召回质量；OTel 库是否恢复活跃维护。

---

### 7. 核心框架与导航：rclcpp `wait_for_message`、rosbag2 EventsCBGExecutor 录制器、Nav2 1.3.13（Humble）

**发生了什么：**
- **rclcpp 新增依赖 node 接口的 `wait_for_message` API（#3230）**：yadunund 提交，让等待消息的能力直接挂在 node 接口上（不再需要额外构造 executor/subscription）
- **rosbag2 录制端替换为 `EventsCBGExecutor`（#2472）**：`rosbag2_py` recorder 的执行器从旧实现迁移到新的事件回调组 executor
- **Nav2 1.3.13（Humble 补丁版，8/21）** 发布；本周 Nav2 提交密集：MPPI 全向速度约束由矩形改椭圆（#6344）随后又被回滚（#6383）、Validate retained path state（#6311）也被回滚（#6382）、AxisGoalChecker 改为从 plan 推导路径方向（#6345）、修复控制环 TF-snapshot 偏差（#6343）、路径变换只查一次 TF（#6375）、Eigen5 编译修复（#6299）
- 其他：ros2_control 新增「卸载所有非活动控制器」（#3466）、launch 修复 `respawn_max_retries` YAML 字符串解析（#969）与 `LaunchService._is_idle`（#996）、rosidl 修复 `BoundedVector::data()`（#978）、Fast-DDS 修复 minor data races（#6422）、rclpy 修复 destroy 后 node 残留（#1711）

**为什么值得关注：** `wait_for_message` 补齐了 rclcpp 一个常用但此前别扭的 API；rosbag2 录制端换到 EventsCBGExecutor 延续了「事件驱动执行器成为默认」的方向（与 rcl_executors 架构议题同源）；Nav2 的「改-回滚」循环说明 MPPI/路径校验改动还在收敛期、Humble 用户宜关注 1.3.13 的实际稳定性。整体是「核心 API 现代化 + 执行器迁移 + 导航栈震荡收敛」的一周。

**影响层面：** 核心框架层 / 导航生态 / 中间件

**下一步盯什么：** `wait_for_message` 是否 backport；EventsCBGExecutor 成为录制端默认后的回归；Nav2 MPPI 椭圆约束与路径校验是否会重新合入；Nav2 1.3.13 在 Humble 的稳定性反馈。

---

## 核心框架更新

### rclcpp
- **新增依赖 node 接口的 `wait_for_message` API（#3230，yadunund，8/21）**：无需自建 executor/subscription 即可等待单条消息。
- **float range bound 无限值测试（#3218，pum1k）**：补测参数 float 边界含无穷值的情形。

**工程意义：** `wait_for_message` 补齐一个高频但别扭的 API，是 rclcpp 可用性的实际提升；后续关注是否 backport。

### rclpy
- **`perf_counter()` 取代 `monotonic()`（#1712，fujitatomoya）**：时间基准一致性。
- **修复 destroy 后 node 残留（#1711，Emerson Knapp）**：解决节点析构生命周期残留。

### rosbag2
- **录制端替换为 `EventsCBGExecutor`（#2472）**：`rosbag2_py` recorder 执行器现代化迁移。

**工程意义：** 录制端换新执行器，与「事件驱动执行器成为默认」的方向一致，也侧面印证 EventsCBGExecutor 的成熟度在提升。

### launch / rcl / rosidl
- **launch：修复 `respawn_max_retries` YAML 字符串解析（#969）**；**修复 `LaunchService._is_idle()` 未考虑后台任务（#996）**。
- **rcl：`rcl_yaml_param_parser` MacOS 构建修复（#1327）**。
- **rosidl：修复 `BoundedVector::data()` 不可用（#978）**。

**工程意义：** 多为长期存在的健壮性/兼容性修复（YAML 解析、跨平台构建、容器 data() 语义），属核心库渐进式打磨。

### rmw / ros2cli / ros2 / rcl_interfaces
- 本周无落在窗口内的功能性 commit。

---

## 生态项目更新

### Navigation2 / Nav2
- **1.3.13（Humble 补丁版，8/21）** 发布。
- MPPI 全向速度约束椭圆化（#6344）+ 回滚（#6383）；Validate retained path（#6311）+ 回滚（#6382）；AxisGoalChecker 从 plan 推导方向（#6345）；控制环 TF-snapshot 偏差修复（#6343）；路径 TF 只查一次（#6375）；Eigen5 编译修复（#6299）；opennav_following 清理（#6367/#6359）。

**工程意义：** 1.3.13 是 Humble 侧跟进；「改-回滚」说明 MPPI 约束与路径校验仍在收敛，社区需关注最终形态。

### ros2_control
- **新增「卸载所有非活动控制器」能力（#3466）**。
- **PR workflow 引入独立 gates（#3474）**：CI 流程质量门控化。
- 控制器文档（#3175）、gz_ros2_control 切 Kilted（#3530）、CI 时序断言放宽（#3535）。
- Kilted 侧控制器系升至 **5.17.0**（control-msgs 6.10.0）。

**工程意义：** 5.17.0 随 Kilted 同步大面积落地；「卸载所有非活动控制器」对多控制器生命周期管理是实用增强。

### MoveIt2
- **2.15.0 已随 Lyrical 8/19 同步入库**（上周发布，本周进稳定 apt）。
- 代码现代化：Qt signal-slot 改函数指针（#3823）、Ubuntu 26.04 CI 修复（#3818）。

**工程意义：** 2.15.0 正式进入 Lyrical 稳定源，MoveIt 面向新工具链的版本落地。

### Gazebo / gz-sim
- `SetComponentData` 用于 Link 速度 setter（#3746）、旋转碰撞体渐变浮力修复（#3880）、时钟 CPU 负载查询（#3816）、GUI 显示命名空间（#3872）、移动机器人+传感器 world 基准（#3846）。

**工程意义：** 仿真器在组件 API、物理、GUI、性能基准上的持续打磨。

### micro-ROS / Fast-DDS
- **Fast-DDS：修复 minor data races（#6422，8/20）**。
- micro-ROS-Agent 本周无新 commit（6.1.1 已在上周发布）。

---

## 工程化观察

1. **发行版进入「双线持续交付」常态**：Lyrical（8/19，104 新/150 更新）与 Kilted（8/21，471 包/39 新）连续完成大规模同步，ROS 2 的两个活跃发行版同步节奏已对齐为「周期性大同步」，选型评估与依赖升级的时间窗口变得可预期。

2. **Physical AI 从散点走向组织化**：OSRA 成立 SIG PAI 并放 demo 仓库 + 路线图，把 LeRobot/仿真器/数据闭环整合成官方工程方向——这是本周最重要的战略信号，值得所有做具身/学习的团队跟踪。

3. **零拷贝/共享内存主线持续推进**：rosidl::Buffer CPU SHM 后端讨论 + Fast-DDS buffer 复用补丁（消除随 buffer 大小的 memset/分配），说明「零拷贝从传输层上移到类型层」正在从理念走向可测量的性能工程。

4. **序列化与 wire 效率实用化加速**：protoros2（protobuf 编排）、以及此前 flatros2/zenoh 等，共同指向「ROS 2 数据出机器人/上云时对载荷与格式的诉求」——off-robot 线效率正成为与车内实时性并列的工程目标。

5. **可观测性升级为「被动、可检索、可审计」**：ros2probe（eBPF 无探针效应）、ROS 2 Inspector（静态架构）、Foxglove 语义检索 + Agents、ros-opentelemetry 回暖，构成一套从抓包到架构到检索到追踪的完整观测栈，且相互可组合。

---

## 前沿议题

### 1. Physical AI 与 ROS 的官方化：SIG PAI 会改变什么
SIG PAI 把「数据采集 → 训练 → 仿真回环 → 部署」整合为官方路线，意味着 ROS 官方资源开始投向量产具身数据闭环。核心张力在于：ROS 作为通用中间件与「具身数据平台」这两种定位如何平衡，以及 demo 仓库能否沉淀为可复用的标准数据管线。

### 2. rosidl::Buffer 的零拷贝边界
CPU SHM 后端若落地，将让「载荷存储共享 + 描述符传输」成为标准能力，与中间件内嵌 SHM（Fast-DDS SHM）形成互补/竞争。问题是 QoS 语义、生命周期与跨进程内存回收如何在类型层保证——这是上周「共享内存生命周期」议题在类型系统的延伸。

### 3. 替代序列化（protobuf）与 ROS 类型系统的摩擦
protoros2 用「三态编排 + 编译期 SSOT 映射」缓解 .msg 与 .proto 的语义鸿沟，但这本质上是绕开而非解决类型系统不统一。当更多 protobuf/替代序列化方案出现，ROS 2 是否需要一等的多序列化一等公民支持，是值得追问的架构问题。

### 4. 执行器收敛与语言惯用的张力
rcl_executors 提案本周仍在社区发酵（详述 7 份 executor、EventsCBGExecutor 转默认、waitable API 对齐 python），与「asyncio 直接执行、语言原生调度」的主张并存。rosbag2 录制端换 EventsCBGExecutor 表明事件驱动方向在落地，但「收敛 vs 语言惯用」的最终取舍尚未定论。

---

## 可继续深挖的问题

1. **SIG PAI 的路线图里程碑具体是什么？** demo 仓库会否沉淀为官方教程/标准数据管线，以及 OSRA 是否会为其配置专职工程资源。

2. **Kilted 首次大同步后，easynav / MOLA / roboplan 在 Kilted 上的稳定性如何？** 它们是否改变「Kilted 上选导航/建图/规划」的推荐组合。

3. **rosidl::Buffer 的 CPU SHM 后端会否有原型落地？** Qualcomm buffer 实现（首个非 NVIDIA）进展如何，以及 Fast-DDS #904 合并后的性能数据。

4. **protoros2 与原生 Protobuf RMW 谁更可能被社区采纳？** 以及替代序列化在 ROS 2 是否会走向一等公民。

5. **ros2probe 能否覆盖 SHM 传输与更多 RMW？** 作为「无探针效应」观测工具，其与 rosbag2 的边界与互补关系如何。

6. **Nav2 的 MPPI 椭圆约束与路径校验会被重新合入吗？** 以及 EventsCBGExecutor 成为 rosbag2 录制端默认后的回归表现。

---

*本摘要基于 ROS Discourse（discourse.ros.org）、ROS 官方周报（2026-08-17）、OSRA SIG PAI 公告、GitHub API（ros2/rclcpp、rclpy、launch、rosbag2、rmw、rcl、rosidl、ros-navigation/navigation2、ros-controls/ros2_control、moveit/moveit2、gazebosim/gz-sim、eProsima/Fast-DDS 等）与 rosdistro 同步公告（Lyrical 2026-08-19、Kilted 2026-08-21）等一手来源整理。*
